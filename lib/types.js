'use strict';

/**
 * Signature or null
 * @typedef {Buffer?} SignatureOption
 */

/**
 * 65 byte buffer.
 * @typedef {Buffer} Signature
 */

/**
 * Public key compressed encoding. (33 bytes)
 * @typedef {Buffer} CompressedPublicKey
 */

/**
 * Proposal payload types when signing
 * requests.
 * @typedef {Number} ProposalPayloadType
 */

